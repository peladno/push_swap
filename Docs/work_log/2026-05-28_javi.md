# 2026-05-28 — Work Log

## English

### What I did

* Continued working on the `push_swap` parser architecture.
* Implemented `is_number()` validation function.
* Implemented `ft_atol()` integration for safe conversion to integer values.
* Added integer range checking using:

  * `INT_MIN`
  * `INT_MAX`
* Implemented duplicate detection using stack traversal.
* Designed and implemented:

  * `create_node()`
  * `stack_add_back()`
* Learned how doubly linked lists work with:

  * `next`
  * `prev`
  * `top`
  * `bottom`
* Understood why `stack_add_back()` should only manage linked list insertion logic.
* Discussed parser responsibilities vs stack responsibilities.
* Reviewed binary radix sort concepts:

  * bit shifting
  * `((num >> bit) & 1)`
  * binary grouping
* Planned future support for:

  * quoted string parsing
  * coordinate compression

### What's next

* Add proper memory cleanup on parser errors.
* Implement `free_stack()`.
* Implement coordinate compression.
* Decide parser behavior for:

  * `./push_swap --simple`
* Discuss quoted string support with partner:

  * `./push_swap "1 2 3"`
* Begin testing parser with Valgrind.

### Blockers / questions for the pair

* Decide whether quoted string parsing will be implemented now or later.
* Decide final parser architecture for mixed input handling.
* Clarify expected behavior for strategy-only execution.
* Decide how coordinate compression module should be organized.

### Time spent

~ 5h

---

# 2026-05-28 — 作業ログ

## 日本語

### やったこと

* `push_swap` の parser アーキテクチャ作業を継続した。
* `parse_args()` の基本構造を実装した。
* `is_number()` のバリデーション関数を実装した。
* `ft_atol()` を使った整数変換を実装した。
* `INT_MIN` / `INT_MAX` を使った範囲チェックを追加した。
* stack 走査による duplicate check を実装した。
* 以下を設計・実装した:

  * `create_node()`
  * `stack_add_back()`
* doubly linked list の構造を理解した:

  * `next`
  * `prev`
  * `top`
  * `bottom`
* `stack_add_back()` が linked list insertion のみ担当するべき理由を理解した。
* parser と stack の責任分離について整理した。
* binary radix sort の基本概念を学習した:

  * bit shift
  * `((num >> bit) & 1)`
  * binary grouping
* 今後追加予定:

  * quoted string parsing
  * coordinate compression

### 次にやること

* parser error 時のメモリ解放を追加する。
* `free_stack()` を実装する。
* coordinate compression を実装する。
* 以下の動作を決める:

  * `./push_swap --simple`
* partner と quoted string support を相談する:

  * `./push_swap "1 2 3"`
* Valgrind を使った parser テストを開始する。

### 詰まったこと・相方への質問

* quoted string parsing を今やるか後でやるか決める必要がある。
* mixed input の parser 設計を最終決定する必要がある。
* strategy-only 実行時の期待動作を確認したい。
* coordinate compression の責任範囲を整理したい。

### 作業時間

約 2 時間
