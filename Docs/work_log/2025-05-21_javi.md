```md
# 2026-05-31 — Work Log

## English

### What I did

- Continued development of the `push_swap` project.
- Reviewed parser architecture and error handling flow.
- Improved understanding of boolean conventions in C:
  - `0 == false`
  - non-zero == true
- Started implementing stack operations.
- Implemented:
  - `swap_top()`
  - `sa()`
  - `sb()`
  - `ss()`
- Reviewed doubly linked list pointer manipulation:
  - `next`
  - `prev`
  - `top`
  - `bottom`
- Started designing the generic `push()` operation.
- Discussed operation return conventions:
  - success/failure
  - status enums
  - operation enums
- Evaluated using:
  - `t_status`
  - `t_operation`
- Planned file organization for operation modules:
  - `swap.c`
  - `push.c`
  - `rotate.c`
  - `reverse_rotate.c`

### What's next

- Implement:
  - `rotate()`
  - `ra()`
  - `rb()`
  - `rr()`
- Implement:
  - `reverse_rotate()`
  - `rra()`
  - `rrb()`
  - `rrr()`
- Test all operations using small stacks.
- Verify pointer integrity after every operation.
- Begin integrating radix sort with compressed indexes.

### Blockers / questions for the pair

- Decide whether to use:
  - integer return values
  - enums (`t_status`, `t_operation`)
- Review final parser behavior for edge cases.
- Confirm operation architecture before implementing all commands.
- Decide if additional helper functions are needed for stack manipulation.

### Time spent

~ 2.5h

---

# 2026-05-31 — 作業ログ

## 日本語

### やったこと

- `push_swap` プロジェクトの開発を継続した。
- parser のアーキテクチャとエラーハンドリングの流れを見直した。
- C における boolean の考え方を理解した:
  - `0 == false`
  - 0以外 == true
- stack operation の実装を開始した。
- 以下を実装した:
  - `swap_top()`
  - `sa()`
  - `sb()`
  - `ss()`
- doubly linked list のポインタ操作について確認した:
  - `next`
  - `prev`
  - `top`
  - `bottom`
- 汎用的な `push()` operation の設計を開始した。
- operation の戻り値設計について検討した:
  - success / failure
  - status enum
  - operation enum
- 以下の enum の利用について検討した:
  - `t_status`
  - `t_operation`
- operation モジュールのファイル構成を計画した:
  - `swap.c`
  - `push.c`
  - `rotate.c`
  - `reverse_rotate.c`

### 次にやること

- 以下を実装する:
  - `rotate()`
  - `ra()`
  - `rb()`
  - `rr()`
- 以下を実装する:
  - `reverse_rotate()`
  - `rra()`
  - `rrb()`
  - `rrr()`
- 小さい stack を使って全 operation をテストする。
- 各 operation 実行後にポインタの整合性を確認する。
- compressed index を利用した radix sort の統合を開始する。

### 詰まったこと・相方への質問

- 戻り値として以下のどちらを使うか決める:
  - int
  - enum (`t_status`, `t_operation`)
- parser の edge case に対する最終的な動作を確認する。
- 全 operation を実装する前にアーキテクチャを確認する。
- stack 操作用の追加 helper function が必要かどうか検討する。

### 作業時間

約 2 時間
```
