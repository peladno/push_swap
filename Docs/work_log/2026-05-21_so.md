# 2026-05-21 — Work Log

## English

### What I did

**Setup & infrastructure**

- Read `en.subject.pdf` (Version 1.1) and created a Japanese translation (`Docs/subject/subject_ja.md`).
- Set up Git: `git init`, registered remote (`github`), pulled the partner's `docs:possible files` commit (empty skeleton + 824-line `README.md`).
- Reviewed the partner's proposed file structure and README plan.
- Built out `Docs/`: `pair_communication/`, `work_log/`, `learning_log/`, `subject/` with bilingual READMEs and templates.
- Drafted the kickoff meeting agenda (`kickoff.md`) with 9 discussion topics, the "three whys", and a tentative role-split table.
- Standardized `learning_log/` filenames to kebab-case (e.g., `algorithms-simple-o-n2.md`).

**Learning — foundations**

- Big-O notation: what it measures, why it ignores constants and low-order terms, growth-rate comparison across complexity classes, and how it applies to push_swap (counting generated operations, not C-level instructions).

**Learning — Simple O(n²) tier**

- Selection Sort, Min Extraction, Insertion Sort, Bubble Sort: concept, push_swap adaptation, operation count, behavior on low disorder.
- Walked through Insertion Sort in detail with concrete traces (`a = [1, 3, 2]` and `a = [4, 2, 5, 1, 3]`).
- Captured analysis in `learning_log/algorithms-simple-o-n2.md` (bilingual).

**Learning — Medium O(n√n) tier**

- Chunk Sort: concept, why `k = √n` is optimal, basic algorithm with a small trace.
- The subject's other three candidates (Block-based partitioning, Bucket Sort with √n buckets, Range-based sorting): general-CS concepts, why all three converge to Chunk Sort in push_swap's operation model.
- In-chunk sort: the rotation-then-`pb` rule (identical to Insertion Sort), why the chunk-bounded search depth keeps it at O(n√n), how cross-chunk order survives cyclic shifts.
- Captured analysis in `learning_log/algorithms-medium-o-nsqrtn.md` (bilingual).

**Pre-meeting proposals decided**

- Simple: Min Extraction or Insertion Sort (final pick depends on how Adaptive uses Simple).
- Medium: Chunk Sort with in-chunk sort.

### What's next

- Schedule the kickoff meeting with Javier.
- Complete pending Git tasks: rename local `master` → `main`, `--set-upstream-to=github/main main`, first commit & push of the `Docs/` tree.
- Study Complex O(n log n) tier (Radix recap + Merge / Quick / Heap comparison).
- Study Adaptive tier (composition design + threshold rationale).
- Resolve the data-structure question (tail pointer? circular doubly linked list?).
- Resolve the mini_sort adoption question.

### Blockers / questions for the pair

- All algorithm selections (every tier) require Javier's input.
- Data structure: does `t_stack` need a `tail` pointer, or do we prefer a circular doubly linked list? The skeleton's `t_stack {top, size}` would force O(n) rotations unless rotated lazily.
- mini_sort: implement or skip? Decision affects Phase 3 of the role split.
- Adaptive: thresholds (0.2 / 0.5) and per-regime complexity targets are fixed by subject VI.3.3 #4. Open question is **which internal algorithm to call in each regime** (Topic 1 picks by default, but e.g. Insertion Sort might fit low-disorder better even if `--simple` uses Min Extraction).

### Time spent

~ Xh *(fill in)*

---

## 日本語

### やったこと

**セットアップ・インフラ**

- `en.subject.pdf`(Version 1.1)を読み、日本語訳を作成(`Docs/subject/subject_ja.md`)。
- Git をセットアップ:`git init`、リモート登録(`github`)、相方の `docs:possible files` コミット(空スケルトン + 824 行の `README.md`)を pull。
- 相方のファイル構成と README プランをレビュー。
- `Docs/` を整備:`pair_communication/`、`work_log/`、`learning_log/`、`subject/`。各サブディレクトリに日英バイリンガル README とテンプレートを配置。
- kickoff ミーティング議題を作成(`pair_communication/2026-05-20_kickoff.md`):9 つの議題、「3 つのなぜ」、役割分担のたたき台テーブル。
- `learning_log/` のファイル名を kebab-case に統一(例:`algorithms-simple-o-n2.md`)。

**学習 — 基礎**

- Big-O 記法:何を測っているか、定数倍と低次項を無視する理由、計算量クラスの増加率比較、push_swap での適用(C 命令ではなく**生成された操作の数**で測る)。

**学習 — Simple O(n²) tier**

- Selection Sort、Min Extraction、Insertion Sort、Bubble Sort:概念、push_swap への適応、操作数、低 disorder 入力での挙動。
- Insertion Sort は具体トレース 2 本(`a = [1, 3, 2]` と `a = [4, 2, 5, 1, 3]`)で動作を確認。
- 分析を `learning_log/algorithms-simple-o-n2.md` にバイリンガルで記録。

**学習 — Medium O(n√n) tier**

- Chunk Sort:概念、`k = √n` が最適バケット数になる理由、basic アルゴリズム + 小さいトレース。
- subject の他 3 候補(Block-based partitioning、Bucket Sort with √n buckets、Range-based sorting):一般 CS としての概念と、push_swap の操作モデル上 Chunk Sort に収束する理由。
- In-chunk sort:rotate-then-`pb` ルール(Insertion Sort と同じ)、chunk 範囲に bound されることで O(n√n) を維持できる理由、循環シフトで cross-chunk 順序が保たれる理由。
- 分析を `learning_log/algorithms-medium-o-nsqrtn.md` にバイリンガルで記録。

**事前ミーティング提案を決定**

- Simple:Min Extraction か Insertion Sort(最終選択は Adaptive で Simple をどう使うかに依存)。
- Medium:Chunk Sort + in-chunk sort。

### 次にやること

- Javier と kickoff ミーティングの日程調整。
- 未完了の Git 作業:ローカルブランチを `master` → `main` にリネーム、`--set-upstream-to=github/main main`、`Docs/` 配下の初コミット & push。
- Complex O(n log n) tier の学習(Radix の復習 + Merge / Quick / Heap の比較)。
- Adaptive tier の学習(composition 設計 + しきい値の根拠)。
- データ構造の議論(tail ポインタ?循環双方向?)。
- mini_sort 採用可否の議論。

### 詰まったこと・相方への質問

- アルゴリズム選定(全 tier)は Javier との議論が必要。
- データ構造:`t_stack` に `tail` を持たせるか、循環双方向にするか?スケルトンの `t_stack {top, size}` のままだと `rotate` 内部で末尾探索が O(n) になる可能性。
- mini_sort:実装するか?Phase 3 の役割分担に影響。
- Adaptive:しきい値(0.2 / 0.5)と各レジームの計算量目標は subject VI.3.3 #4 で固定。**開いている論点は「各レジームで何を呼ぶか」**(Topic 1 の選定がデフォルトだが、例えば低 disorder には Insertion Sort のほうが適しているかもしれない ——`--simple` フラグが Min Extraction を使っていても)。

### 作業時間

約 X 時間 *(後で記入)*
