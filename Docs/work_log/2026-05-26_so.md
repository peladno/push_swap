# 2026-05-26 — Work Log

## English

### What I did

**Pair coordination — reviewed Javier's contributions**
- `learning_log/array&linkedlists.md` — initial data-structure proposal. Flagged that the text says "double linked list" but the struct is **singly** linked (`*next` only, no `*prev`), and that no `tail` pointer means `ra/rb/rra/rrb` would degrade to O(n).
- `learning_log/radix_sort.md` — solid general explanation of Radix Sort with code skeleton, but **coordinate compression is missing** (the key to the O(n log n) push_swap claim). Also `t_stack` design inconsistent with his other file.

**Learning — Complex tier**
- Studied all five subject candidates: Radix Sort (LSD/MSD), Merge Sort (2-stack), Quick Sort (stack partitioning), Heap Sort, Binary Indexed Tree.
- Concluded that Radix LSD dominates push_swap's operation model; Heap can't even achieve O(n log n) in this model; BIT isn't a sorting algorithm by itself.
- Created `learning_log/algorithms-complex-o-nlogn.md` (bilingual).
- Decided pre-meeting proposal: **Complex = Radix LSD (binary)**.

**Learning — data structure**
- Created `learning_log/linked-list-design.md` (bilingual) consolidating the pair's analysis. Documents why doubly linked + tail (Option A) or circular doubly linked (Option B) is required, why Javier's current proposal degrades rotate ops, and why "linked list is slower than array" is the wrong framing for push_swap.

**Adaptive tier — recognized and fixed two misframings**
1. Treated the `0.2 / 0.5` thresholds as discretionary — they're subject-mandated (VI.3.3 #4).
2. Treated the disorder calculation as a design choice — subject provides the pseudo-code (VI.3.2).
- Updated `kickoff.md` Topic 6 to reflect what's actually fixed vs open:
  - **Fixed**: thresholds, regime complexity targets, disorder pseudo-code.
  - **Open**: which internal algorithm per regime; README documentation responsibility.
- Created `learning_log/algorithms-adaptive.md` (bilingual).

**kickoff.md updates (pre-meeting)**
- Added Complex tier proposal (Radix LSD) under Topic 1, with rationale.
- Updated Context's pre-meeting preparation note to cover Complex.
- Reframed Topic 6 to focus on the real design question (Simple-algorithm choice for the low-disorder regime).
- Removed disorder calculation bullet from Topic 6 (it's an implementation detail, not a meeting topic).

**Kickoff meeting with Javier**
- Held the first sync meeting and reached decisions on Topics 1, 2, 3, 4, and Topic 8 (partial):
  - **Topic 1 (Algorithms)**: Simple = **Insertion Sort**; Medium = **Chunk Sort with in-chunk sort**; Complex = **Radix Sort LSD (binary)**; Adaptive = direct dispatch to tier algorithms.
  - **Topic 2 (Extras)**: mini_sort **NOT implemented** (out of subject scope).
  - **Topic 3 (Role split)**: Phase 1, 2 → parallel-draft-then-compare; Simple & Medium → Javier; Complex → So.
  - **Topic 4 (Data structure)**: **Option A** — doubly linked list + `tail` pointer.
  - **Topic 8 partial (Git)**: parallel phases use one branch per partner; other details pending.
- Updated `kickoff.md` to a partial post-meeting record reflecting all decisions, removed `t_stack` "tentative" notes, refreshed Three Whys with concrete answers where possible.

**Topic 5 decided: Approach A (universal coordinate compression)**
- Discussed whether to apply coordinate compression for all tiers or only for Complex.
- Decided **Approach A**: compression at parser stage for all tiers; all algorithms work on `t_node->index`.
- Rationale: code uniformity, Norm-friendliness, Chunk Sort benefits from uniform value range, defensible explanation.
- Updated `kickoff.md` Topic 5 (PENDING → DECIDED) with a supporting table per tier.

**Implementation plans directory established**
- Identified the noise problem in `learning_log/` (includes rejected algorithms; hard to use as a current-design reference during implementation).
- Created `Docs/implementation_plans/` to hold focused **build contracts for adopted designs only**.
- Created 8 files (bilingual):
  - `README.md` (folder purpose + conventions + plan index)
  - `TEMPLATE.md` (blank starter for future plans)
  - `data-structure.md`, `coordinate-compression.md`, `simple-insertion-sort.md`, `medium-chunk-sort.md`, `complex-radix-lsd.md`, `adaptive-dispatcher.md`
- Each plan contains: metadata, scope, function-signature interfaces, step-by-step implementation outline (pseudo-code), edge cases, complexity argument, testing strategy. **No full C function bodies** (per agreed granularity policy).

### What's next

- **Phase 1 implementation**: both partners independently draft `data-structure` + `coordinate-compression` + parser on separate branches, then compare and merge.
- **Phase 2 implementation**: same parallel-draft workflow for the 11 push_swap operations.
- **Follow-up meeting**: resolve Topic 7 (code conventions), Topic 9 (milestones), and Topic 8 details (branch naming, review process, commit message format, merge strategy).
- **Resolve implementation detail**: does `t_stack` hold A and B together, or as separate instances? Decide before Phase 1 implementation starts.
- **README scaffolding**: login attribution, section structure, complexity arguments per algorithm.

### Blockers / questions for the pair

- Topic 7 (code conventions): still open. Need follow-up meeting (Norm, file/function naming, libft placement, operation wrapper signature).
- Topic 8 details: branch naming convention, review process, commit message format, merge strategy for non-parallel phases, where the final-version branch lives.
- Topic 9 (milestones): need a campus deadline + per-phase target dates.
- `t_stack` A/B combined or separate instances? (Implementation detail, decide before Phase 1 implementation.)
- README writing breakdown: how the documentation responsibilities are split in finer detail.

### Time spent

~ Xh *(fill in)*

---

## 日本語

### やったこと

**ペア連携 — Javier の追加分をレビュー**
- `learning_log/array&linkedlists.md` — データ構造の初期提案。テキストでは「double linked list」と言いつつ struct は **single linked**(`*next` のみ、`*prev` 無し)、かつ `tail` ポインタが無いため `ra/rb/rra/rrb` が O(n) に劣化する点を指摘。
- `learning_log/radix_sort.md` — Radix Sort 一般の解説とコード骨格が丁寧に書かれているが、**座標圧縮への言及が無い**(push_swap で O(n log n) を主張する根拠そのもの)。`t_stack` の設計も彼自身の他ファイルと不一致。

**学習 — Complex tier**
- subject の 5 候補すべてを学習:Radix Sort (LSD/MSD)、Merge Sort (2-stack)、Quick Sort (stack partitioning)、Heap Sort、Binary Indexed Tree。
- 結論:push_swap 操作モデルでは Radix LSD が支配的。Heap はこのモデルでは O(n log n) すら達成できない。BIT はそもそも算法ではない。
- `learning_log/algorithms-complex-o-nlogn.md` をバイリンガルで作成。
- 事前提案を決定:**Complex = Radix LSD(2 進)**。

**学習 — データ構造**
- `learning_log/linked-list-design.md` をバイリンガルで作成。双方向リスト + tail(Option A)または循環双方向(Option B)が必要な理由、Javier の現提案では rotate 系が劣化する根拠、「linked list は array より遅い」というフレーミングが push_swap では誤りである理由を整理。

**Adaptive tier — 自分の 2 つのフレーミングミスを修正**
1. しきい値 `0.2 / 0.5` を学習者の判断扱いしていたが、subject 規定(VI.3.3 #4)の固定値。
2. disorder 計算を設計判断扱いしていたが、subject が pseudo-code を提供(VI.3.2)。
- `kickoff.md` Topic 6 を「何が固定で、何が開いているか」が分かる形に更新:
  - **固定**:しきい値、各レジームの計算量要件、disorder pseudo-code。
  - **開いている**:各レジームで呼ぶアルゴリズム、README 文書化の役割分担。
- `learning_log/algorithms-adaptive.md` をバイリンガルで作成。

**kickoff.md 更新(ミーティング前)**
- Topic 1 に Complex tier の提案(Radix LSD)を根拠付きで追加。
- Context の pre-meeting preparation note を Complex にも対応。
- Topic 6 を「実質的な設計判断(Adaptive 低 disorder で呼ぶ Simple)」に絞って書き直し。
- Topic 6 から disorder 計算の bullet を削除(実装時の判断、ミーティング議題ではない)。

**Javier との kickoff ミーティング**
- 初の同期ミーティングを開催し、Topic 1, 2, 3, 4 と Topic 8(部分)で決定:
  - **Topic 1(アルゴリズム)**:Simple = **Insertion Sort**、Medium = **Chunk Sort + in-chunk sort**、Complex = **Radix Sort LSD(2 進)**、Adaptive = tier algorithm への直接ディスパッチ。
  - **Topic 2(extras)**:mini_sort **実装しない**(subject 範囲外)。
  - **Topic 3(役割分担)**:Phase 1, 2 → 並行ドラフト → 比較 → 統合、Simple と Medium → Javier、Complex → So。
  - **Topic 4(データ構造)**:**Option A** —— 双方向連結リスト + `tail` ポインタ。
  - **Topic 8 部分(Git)**:並行 Phase は各自 1 ブランチ、その他は未決定。
- `kickoff.md` を partial post-meeting record として全決定を反映する形に更新。役割分担テーブルの「暫定」注記を削除、Three Whys の具体回答化(可能な箇所のみ)。

**Topic 5 決定:Approach A(全 tier 共通の座標圧縮)**
- 座標圧縮を全 tier に適用するか Complex のみかを議論。
- **Approach A** を決定:parser stage で全 tier 共通の圧縮、全 algorithm は `t_node->index` で動作。
- 根拠:コードの一貫性、Norm との相性、Chunk Sort が均等値域から恩恵、ディフェンスで説明しやすい。
- `kickoff.md` の Topic 5 を PENDING → DECIDED に更新、tier 別の位置付け表を追加。

**`implementation_plans/` ディレクトリ新設**
- `learning_log/` のノイズ問題(不採用算法も含むため、実装中の現行設計リファレンスとしては使いにくい)を認識。
- 新規 `Docs/implementation_plans/` を作成 —— **採用設計の実装契約のみ**を集約。
- 8 ファイル作成(バイリンガル):
  - `README.md`(フォルダの目的 + 規約 + プラン一覧)
  - `TEMPLATE.md`(新規プラン用の空テンプレート)
  - `data-structure.md`、`coordinate-compression.md`、`simple-insertion-sort.md`、`medium-chunk-sort.md`、`complex-radix-lsd.md`、`adaptive-dispatcher.md`
- 各プランは:metadata、scope、関数シグネチャ・インターフェース、段階的な実装手順(擬似コード)、edge cases、計算量議論、テスト戦略 を含む。合意した粒度方針どおり、**完全な C 関数本体は含まない**。

### 次にやること

- **Phase 1 実装**:両者がそれぞれのブランチで `data-structure` + `coordinate-compression` + parser を独立にドラフト、その後比較して統合。
- **Phase 2 実装**:同じ並行ドラフト workflow で 11 個の push_swap 命令。
- **Follow-up ミーティング**:Topic 7(コード規約)、Topic 9(マイルストーン)、Topic 8 詳細(ブランチ命名、レビュー、コミットメッセージ形式、マージ戦略)を解決。
- **実装詳細の決定**:`t_stack` で A と B を統合するか別 instance にするか?(Phase 1 着手前に決定)
- **README の枠組み作成**:ログイン名記載、節構成、各アルゴリズムの計算量議論。

### 詰まったこと・相方への質問

- Topic 7(コード規約):未着手。follow-up ミーティングで詰める(Norm、ファイル/関数命名、libft 配置、operation ラッパー署名)。
- Topic 8 詳細:ブランチ命名規則、レビュープロセス、コミットメッセージ形式、並行 Phase 以外のマージ戦略、最終版ブランチの置き場所。
- Topic 9(マイルストーン):キャンパス締切 + 各 Phase の目標期日。
- `t_stack` で A/B を統合するか別 instance か?(実装詳細、Phase 1 実装前に決定)
- README 執筆の細分化:文書化の役割分担をもう一段細かく。

### 作業時間

約 X 時間 *(後で記入)*
