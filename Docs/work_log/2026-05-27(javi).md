# 2026-05-27— Work Log

## English

### What I did

* Started implementing the `push_swap` parser.
* Created the base parser structure using `parse_args()`.
* Implemented strategy flag detection:

  * `--simple`
  * `--medium`
  * `--complex`
  * `--adaptive`
* Learned how to use `ft_strcmp()` to compare flags.
* Understood why the default strategy should be `STRATEGY_ADAPTIVE`.
* Began designing the parser flow:

  * parse flags
  * validate tokens
  * convert numbers
  * build stack `a`
* Reviewed how radix sort works using binary operations.
* Learned how `((num >> bit) & 1)` extracts bits for radix sort.
* Planned the parser architecture with small helper functions.

### What's next

* Implement `is_valid_token()`
* Implement safe integer conversion with `ft_atol_safe()`
* Handle quoted string input:

  * `"1 2 3"`
* Implement duplicate detection
* Build stack creation functions
* Start coordinate compression


### Time spent

~ 4h

---

# 2026-05-27 — 作業ログ

## 日本語

### やったこと

* `push_swap` のパーサー実装を開始した。
* `parse_args()` を使ってパーサーの基本構造を作成した。
* 戦略フラグ検出を実装した:

  * `--simple`
  * `--medium`
  * `--complex`
  * `--adaptive`
* `ft_strcmp()` を使ったフラグ比較を理解した。
* デフォルト戦略を `STRATEGY_ADAPTIVE` にする理由を理解した。
* パーサーの流れを設計し始めた:

  * フラグ解析
  * トークン検証
  * 数値変換
  * stack `a` 作成
* binary radix sort の仕組みを学習した。
* `((num >> bit) & 1)` によるビット取得方法を理解した。
* 小さな helper 関数を使ったパーサー構成を計画した。

### 次にやること

* `is_valid_token()` を実装する
* `ft_atol_safe()` を実装する
* `"1 2 3"` のような quoted string を処理する
* 重複チェックを実装する
* stack 作成関数を実装する
* coordinate compression を開始する

### 作業時間

約 4 時間
