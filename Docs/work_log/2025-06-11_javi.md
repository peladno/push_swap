# 2026-06-11 — Work Log

## English

### What I did
- Completed the implementation and testing of the core `chunk_sort.c` algorithm (`count_elements_in_chunk`, `find_first_in_chunk_pos`, `rotate_a_to_pos`, `get_target_position_b`, `rotate_b_to_target`, `align_stack_b`, and `chunk_sort`).
- Verified that the logic efficiently sorts a medium tier of integers (e.g., 100 elements benchmark) using coordinate compression (`index`) with an optimized $O(n\sqrt{n})$ time complexity.
- Refactored several nested blocks and long loops to align with the strict 42 school Norminette constraints (ensuring functions do not exceed 25 lines).
- Prepared conceptual strategies and theoretical answers regarding chunk sizes ($c = \lceil\sqrt{n}\rceil$) to defend the design confidently during the Live Code evaluation.


### Blockers / questions for the pair
- Regarding Norminette optimization for `chunk_sort.c`, should we extract the dynamic mathematical calculation for the chunk size into a dedicated external utility helper function (`get_dynamic_chunk_size`)? This would avoid calling `<math.h>`'s `ceil` or `sqrt` and prevent adding local variables, keeping the main loop cleaner for the evaluator.

### Time spent
~ 4.5h

---

## 日本語

### やったこと
- `chunk_sort.c` のコアアルゴリズムの実装およびテストを完了（`count_elements_in_chunk`、`find_first_in_chunk_pos`、`rotate_a_to_pos`、`get_target_position_b`、`rotate_b_to_target`、`align_stack_b`、`chunk_sort` の各関数）。
- 座標圧縮（`index`）を活用し、100個の整数ベンチマーク（Medium Tier）を $O(n\sqrt{n})$ の最適化された時間計算量で効率的にソートできるロジックを検証。
- 42の厳格な規約（Norminette）に対応するため、関数が25行を超えないよう、ネストされたブロックや長いループのモジュール化・リファクタリングを実施。
- 共通テストや対面評価（Live Code）で突っ込まれやすい「チャンクサイズ（$c = \lceil\sqrt{n}\rceil$）の決定根拠」について、ロジックの解説 and 防衛用（ディフェンス）の理論的回答を整理。


### 詰まったこと・相方への質問
- `chunk_sort.c` のNorminette対策について：チャンクサイズを動的に計算する処理を、外部の小さな共通ユーティリティ関数（`get_dynamic_chunk_size` など）として別ファイルに切り出すのはどうだろう？ そうすれば `<math.h>` の `ceil` や `sqrt` を呼ばずに済み（Makefileの `-lm` リンクエラーも防げる）、ローカル変数を減らしてメイン関数をスッキリさせられるので、評価者への印象も良くなりそう。

### 作業時間
約 4.5 時間