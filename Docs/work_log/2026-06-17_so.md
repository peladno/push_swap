# 2026-06-17 — Work Log

## English

### What I did

Prepared the submission, took the first peer evaluation (NOT PASS), and turned the
feedback into a concrete fix plan.

**README — review and full rewrite**

- Reviewed Javier's README and found it described the strategy selection by **input
  size (N)** — but the code selects by **disorder** (the subject-mandated 0.2 / 0.5
  thresholds). README contradicted `dispatch.c`.
- Also missing/incorrect: the required threshold rationale + per-regime time/space
  complexity (subject VI.3.2 / VII), the four strategies / `--bench` / disorder were
  unmentioned, `checker` was claimed to exist (it doesn't), and a leftover AI-chat
  line ("If you'd like, I can now run make…") was left in.
- Rewrote it to match the implementation: disorder-based adaptive selection, the
  four strategies + flags, `--bench` with a real output example, accurate algorithm
  descriptions (insertion-into-B, chunk √n, binary LSD radix), per-regime complexity
  table, and an honest AI-usage section. English then Japanese in one file. Matched
  the `--bench` example to the real program output byte-for-byte.

**Submission repo (`42git`) cleanup**

- The 42 submission is a separate repo (`42git`, master) from our GitHub. It still
  carried the whole `Docs/` folder (52 files: work logs, plans, learning logs, the
  subject). Removed `Docs/` from the submission (kept on GitHub) and synced the new
  README. Verified the code there is current (dispatch / strategy_label / bench all
  present) and builds + sorts. Committed and pushed; the submission now holds only
  Makefile / sources / headers / libft / README. No build artifacts tracked
  (`.gitignore` already covers `*.o` / `*.a` / `push_swap`).

**First peer evaluation — NOT PASS**

Feedback:
1. README didn't document the **work split** between me and Javier.
2. **`total_ops` too high for small `n`** — requirement `n=3 ≤ 5`, `n=5 ≤ 15`.
   Measured: `1 2 3 → 6`, `2 1 3 → 7`, `3 2 1 → 10`, `5 4 3 2 1 → 25`. The
   disorder dispatch sends small/high-disorder inputs to radix/chunk, which are
   far from minimal for tiny `n`.
3. **Already-sorted input (disorder = 0) still emits ops** — should be zero. (`1 2 3
   → 6`.)
4. Live coding (a flag that prints only `total_ops`): I triggered it on
   `bench->enabled == -2`. Passed, but the magic value can't coexist with `--bench`.

**Fix plan (recorded in `pair_communication/2026-06-17_review-not-pass-fix-plan.md`)**

- **FB3:** reuse the already-computed `disorder` — early `return` in `dispatch` when
  `disorder == 0` (≡ zero inversions ≡ sorted). No separate `is_sorted` scan;
  `== 0.0` is exact here (`0 / pairs` is exactly `0.0`). Idea from re-thinking with
  the disorder metric I already had.
- **FB2:** add `sort_three` / `sort_five` and route by **size before disorder** in
  `dispatch` (`disorder==0` → size ≤ 3 → size ≤ 5 → else disorder dispatch). The
  disorder regimes (VI.3) govern the general case; small-`n` limits and the sorted
  rule are separate baseline requirements — size-priority doesn't remove disorder
  dispatch (`n ≥ 6`).
- **FB1:** add a README "Contributions" section. Corrected the work split against
  `kickoff.md`: stack / parser / operations are **joint** (parallel-draft →
  integrated); Javier = Simple + Medium; So = Complex + Adaptive + Benchmark. Noted
  the deltas from the kickoff plan (benchmark was planned split but I took it whole;
  adaptive planned "both" but I led it).
- **Live coding (next time):** a dedicated `silent` flag + an `emit` helper instead
  of the magic value.

### What's next

- Implement the `disorder == 0` early return in `dispatch` (FB3).
- Implement `sort_three` / `sort_five` + size-priority routing (FB2).
- Add the README "Contributions" section once Javier confirms the split (FB1).
- Re-read subject VI.3 / VI.4 with Javier to confirm the small-`n` reconciliation.
- Later: the clean `silent` flag + `emit` helper for the live-coding flag.

### Blockers / questions for the pair

- Javier to confirm/correct the work split (esp. README authorship + the benchmark
  hand-over).
- Agreement on small-`n` size-priority vs the disorder regimes (subject reading).

### Time spent

~ Xh *(fill in)*

---

## 日本語

### やったこと

提出準備をし、初回のピア評価(不合格)を受け、FB を具体的な修正計画に落とした。

**README —— レビューと全面書き直し**

- Javier の README をレビューし、戦略選択を **入力サイズ(N)基準**で説明していると判明
  —— しかしコードは **disorder 基準**(subject 必須の 0.2 / 0.5 しきい値)。README が
  `dispatch.c` と矛盾。
- 欠落/誤りも:必須のしきい値根拠 + 各レジームの時間/空間計算量(subject VI.3.2 /
  VII)、4 戦略 / `--bench` / disorder が未記載、`checker` が「ある」と虚偽記載、AI
  チャットの残骸(「If you'd like, I can now run make…」)が残存。
- 実装に忠実に書き直し:disorder 基準の適応選択、4 戦略 + フラグ、`--bench`(実出力
  例つき)、正確なアルゴリズム説明(B への挿入・チャンク √n・2 進 LSD radix)、各レジーム
  計算量表、正直な AI 使用節。英→日を 1 ファイルに。`--bench` 例は実プログラム出力と
  1 バイト単位で一致させた。

**提出 repo(`42git`)の整理**

- 提出は GitHub とは別 repo(`42git`、master)。`Docs/` 一式(52 ファイル:work log・
  plan・learning log・subject)が残っていた。`Docs/` を提出から削除(GitHub には残す)、
  新 README を同期。コードが最新(dispatch / strategy_label / bench あり)でビルド・
  ソート可を確認。コミット & push。提出は Makefile / sources / headers / libft / README
  のみに。ビルド成果物は未追跡(`.gitignore` が `*.o` / `*.a` / `push_swap` を網羅)。

**初回ピア評価 —— 不合格**

FB:
1. README に自分と Javier の **分担**が未記載。
2. **小 `n` で `total_ops` 過大** —— 要件 `n=3 ≤ 5`、`n=5 ≤ 15`。実測:`1 2 3 → 6`、
   `2 1 3 → 7`、`3 2 1 → 10`、`5 4 3 2 1 → 25`。disorder dispatch が小/高 disorder の
   入力を radix/chunk に送り、小 `n` で最小から程遠い。
3. **整列済み(disorder = 0)でも操作が出る** —— 0 が正(`1 2 3 → 6`)。
4. ライブコーディング(`total_ops` のみ出力するフラグ):`bench->enabled == -2` で発火。
   合格したがマジック値で `--bench` と両立しない。

**修正計画(`pair_communication/2026-06-17_review-not-pass-fix-plan.md` に記録)**

- **FB3:** 既に計算済みの `disorder` を再利用 —— `dispatch` で `disorder == 0`
  (≡ 転倒 0 ≡ 整列済み)のとき早期 return。別途 `is_sorted` 不要、`== 0.0` はここでは
  厳密(`0 / pairs` は正確に `0.0`)。手元の disorder 指標を見直して得た案。
- **FB2:** `sort_three` / `sort_five` を追加し、`dispatch` で **size を disorder より
  優先**ルーティング(`disorder==0` → size ≤ 3 → size ≤ 5 → それ以外 disorder dispatch)。
  disorder レジーム(VI.3)は一般ケースの規定で、小 `n` 上限と整列済みルールは別の
  baseline 要件 —— size 優先は disorder dispatch を消さない(`n ≥ 6`)。
- **FB1:** README に "分担" 節を追加。`kickoff.md` に照らし分担を修正:stack / parser /
  operations は **共同**(並行ドラフト → 統合)、Javier = Simple + Medium、So = Complex
  + Adaptive + Benchmark。kickoff 計画との差分も明記(benchmark は分担計画だったが So が
  全体引き取り、Adaptive は計画「両者」だが So 主導)。
- **ライブコーディング(次回):** マジック値でなく専用 `silent` フラグ + `emit` ヘルパー。

### 次にやること

- `dispatch` で `disorder == 0` 早期 return を実装(FB3)。
- `sort_three` / `sort_five` + size 優先ルーティングを実装(FB2)。
- Javier の分担確認後、README に "分担" 節を追加(FB1)。
- subject VI.3 / VI.4 を Javier と再読し、小 `n` の整合を確認。
- 後日:ライブコーディング用フラグをクリーンな `silent` + `emit` ヘルパーで。

### 詰まったこと・相方への質問

- Javier に分担の確認/修正(特に README 著者行 + benchmark の引き継ぎ)。
- 小 `n` の size 優先 vs disorder レジームの整合(subject 解釈)の合意。

### 作業時間

約 X 時間 *(後で記入)*
